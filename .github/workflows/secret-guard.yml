name: Secret Guard

on:
  pull_request:
  push:

jobs:
  secret-guard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Guard against hardcoded secrets/personal data
        run: bash scripts/check-no-secrets.sh --full
